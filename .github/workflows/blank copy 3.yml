name: Example Workflow

on:
  push:
    branches:
      - test

jobs:
  example_job:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Print repository value
      run: 
           echo "${{ github.repository "}}
           echo "${{ github.repository "}}
           echo "${{ github.repository "}}
           echo "${{ github.repository "}}
           echo "${{ github.repository "}}
           echo "${{ github.repository "}}
